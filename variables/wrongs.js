

let $game = 1; // объявили переменную с именем "$"

let _version = 2; // а теперь переменную с именем "_"

let 1game; // не может начинаться с цифры

let my-Game; // дефис '-' не разрешён в имени

let let = 5; // нельзя назвать переменную "let", ошибка!

let return = 5; // также нельзя назвать переменную "return", ошибка!